#include "main.h"
/**
*_abs - function that computes the absolute value of integer
*
*@n: the int to check
*
*Return: Always 0 (Success)
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}


