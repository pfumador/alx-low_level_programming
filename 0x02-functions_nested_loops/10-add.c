#include "main.h"
/**
*add - function adds two integers
*Description: adds two integers
*@a; first int to add
*@b: second int to add
*Return: results
*/
int add(int a, int b)
{
	int results;

	results = a + b;
	return (results);
}
